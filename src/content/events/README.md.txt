Sample entries — replace with real events (open item 4).

Rules enforced by the schema and the render path:
  • `client` renders ONLY when `clientPublic: true`. Otherwise the entry shows
    as "sector, city, year", which still carries weight.
  • `photo` without `photoConsent: true` FAILS THE BUILD. That is deliberate.

No real client name is published here. The one named entry below is an obvious
placeholder so the cleared path is visible; replace or delete it.
